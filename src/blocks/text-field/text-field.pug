mixin text-field({ type = 'text', name, theme, isMasked, placeholder, disabled = false, readonly = false, cursor, } = {})

  - let darkThemeModifier = ''
  - let maskedThemeModifier = ''
  - let cursorTypeModifier = ''

  case theme
    when 'dark'
      - darkThemeModifier = ' '+'text-field_dark'
    
  if isMasked
    - maskedThemeModifier = ' '+'text-field_masked'
    - placeholder = 'ДД.ММ.ГГГГ'

  case cursor
    when 'pointer'
      - cursorTypeModifier = ' '+'text-field_clickable'

  input(
    type=type
    name=name
    class=`text-field js-text-field${darkThemeModifier}${maskedThemeModifier}${cursorTypeModifier}`
    placeholder=placeholder
    disabled=disabled
    readonly=readonly
  )