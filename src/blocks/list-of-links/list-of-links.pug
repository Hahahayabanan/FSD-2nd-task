include ../material-icons/material-icons.pug

mixin list-of-links({ links = [] } = {})
  
  ul.list-of-links
    each link in links
      li.list-of-links__item(class=link.isDropdown ? 'list-of-links__item_dropdown' : '')
        if link.href === undefined
          - link.href = '/mock-address/change-me'
        if link.text === undefined
          - link.text = 'click me'
        a.list-of-links__link(href=link.href class=link.isActive ? 'list-of-links__link_active' : '')=link.text
        if link.isDropdown
          .list-of-links__arrow-down
            +material-icons({
              type: 'keyboard_arrow_down', 
              color: 'shade',
            })